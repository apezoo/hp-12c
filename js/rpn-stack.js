/**
 * HP-12C RPN Stack Engine
 * Manages 4-level stack (X, Y, Z, T registers) with automatic lift/drop
 */

class RPNStack {
    constructor() {
        this.x = 0;
        this.y = 0;
        this.z = 0;
        this.t = 0;
        this.lastX = 0;
    }

    // Core stack operations - to be implemented in Phase 2.1
}
