/**
 * HP-12C Memory Manager
 * Manages 20 storage registers (R0-R19)
 */

class MemoryManager {
    constructor() {
        this.registers = Array(20).fill(0);
    }

    // Memory operations - to be implemented in Phase 2.4
}
